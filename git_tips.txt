generate keygen = ssh-keygen -t rsa -b 4096 -c "github email add"

#checking ssh is running or not
eval $(ssh-agent -s)

#add ssh private key
ssh-add ~/.ssh/id_rsa

#copy public ssh key and paste it in the github account

git add filename/foldername
git commit -m "commit message"
git bramch -M branchname

updating the file content

git remote add origin http-url
git push -u origin main

edited using the remote text editor
